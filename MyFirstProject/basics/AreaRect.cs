using System;
using System.Collections.Generic;
using System.Text;

namespace MyFirstProject.basics
{
    class AreaRect
    {
        static void Main(string[]args)
        {
            int length = 5, breadth = 8,area;
            area = length * breadth;
            Console.WriteLine("area of rectangle is " + area);
            
        }
    }
}
